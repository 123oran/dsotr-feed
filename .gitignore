# OS / Google Drive cruft
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*~

# Node (only if tooling is added later)
node_modules/
npm-debug.log*

# Vercel
.vercel

# Secrets (never commit real tokens)
.env
.env.local
.env*.local

# Renderer tooling — installed/generated locally, not committed
render/node_modules/
render/.tmp/
*.webm

# Rendered clips are hosted on GitHub Releases (too big for git / Vercel Hobby),
# not committed. Regenerate with render/, upload with render/upload-clips.mjs.
media/*.mp4
