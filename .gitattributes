# Normalize text line endings to LF — this repo is served on Linux (Vercel)
* text=auto eol=lf

# Binary assets — never diff or convert
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ttf  binary
*.otf  binary
*.woff  binary
*.woff2 binary
